application:
  websocket:
    session:
      end-point: websocket
      allowed-origin: "*"
      destination-prefix: /topic
      user-destination-prefix: /user
      application-destination-prefix: /app
      error: /error
